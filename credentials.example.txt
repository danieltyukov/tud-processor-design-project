# PDP Project — Group 24 credentials TEMPLATE
#
# Copy this file to `credentials.txt` (in this same directory) and fill in
# the values, OR drop a populated copy at `~/.config/pdp-project-24/credentials.txt`
# (the historical default location). Either path works — see scripts/_lib.sh.
#
# After creating it:    chmod 600 credentials.txt
# It is .gitignored — never commit a populated copy.
#
# You can also override the location explicitly:
#     export PDP_CREDS_FILE=/path/to/credentials.txt

# --- PDP development server (ce-procdesign01.ewi.tudelft.nl) ---
# Shared group account; the password is on Brightspace / Teams (group 24).
PDP_SERVER_HOST=ce-procdesign01.ewi.tudelft.nl
PDP_SERVER_USER=cese4040-24
PDP_SERVER_PASS=

# Path to YOUR ssh private key (the matching .pub gets pushed by setup-server-auth.sh).
# Use an existing key or generate one: ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519_tudelft
PDP_SSH_KEY=$HOME/.ssh/id_ed25519_tudelft

# Group number drives x2go session naming etc.
PDP_GROUP_NUMBER=24

# --- gitlab.ewi.tudelft.nl personal access token ---
# Used on the SERVER side for HTTPS-git (port 22 is blocked outbound there).
# Create at: https://gitlab.ewi.tudelft.nl/-/profile/personal_access_tokens
# Scopes needed: read_repository + write_repository
# Each teammate makes their own token; do NOT share.
PDP_GITLAB_USER=
PDP_GITLAB_TOKEN=
